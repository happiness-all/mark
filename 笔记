export function Compress(img: any, type: any, callback: Function) {
  let canvas: any = document.createElement("canvas");
  let context: any = canvas.getContext("2d");
  let size = checkSize(img.width, img.height);
  let width = size.width;
  let height = size.height;
  canvas.width = width;
  canvas.height = height;
  // 开始画图
  context.fillStyle = "#fff";
  context.fillRect(0, 0, canvas.width, canvas.height);
  context.drawImage(img, 0, 0, canvas.width, canvas.height);
  let base64Data = canvas.toDataURL("image/jpeg", 0.5); // 压缩图片
  canvas = context = null;
  img = null;
  callback(base64Data);
  // return base64Data
}
filter(item=>item) 去除掉不合法数组
if('development'===process.env.NODE_ENV){
  let cookies="SITUCSSOSESSION=NDA2MmY4MWQtZDgzOS00ZGRhLWIzNTUtNjcyMmJjNzhmOTI1; XSRF-TOKEN=FB8F3A7A71EB684970C06913E930E9B0E78ECABC905E9E09; UCMARKETPRESSION=Y2U2MGE4NGYtYzE3OC00YmNmLTkxZDUtOTY1Nzk4ZDhmZDIz; dcpLocaleResolver=zh-CN; login_uuid=9502a497-a39f-4088-8cf6-fa51024b0f24; JSESSIONID=65E71FD566B5291D99FAA3AB81F93072";
  cookies.split(';').map(str=>str.trim()).forEach(item=>{
    document.cookie=item
  })
}
   this.views.forEach(tm => {
        this.$set(tm, "actived", false);
      }); 为每一个添加一个新的观察视图


#### HTML的学习之路

使用iframe 的时候，它的高度和宽度不是自适应的，需要自己设置width：100%；height:100%

##### IE浏览器里滚动条的样式

| 滚动条样式                  | 可否继承 | 描述                                   |
| --------------------------- | -------- | -------------------------------------- |
| scrollbar-3dlight-color     | yes      | 设置滚动框的和滚动条箭头坐上边缘的颜色 |
| scrollbar-highlight-color   | yes      | 设置滚动框的和滚动条箭头左上边缘的颜色 |
| scrollbar-face-color        | yes      | 设置滚动框和滚动条箭头的颜色           |
| scrollbar-arrow-color       | yes      | 设置滚动条箭头的颜色                   |
| scrollbar-shadow-color      | yes      | 设置滚动框的和滚动条箭头右下边缘的颜色 |
| scrollbar-dark-shadow-color | yes      | 设置滚动公调主要构成部分的颜色         |
| scrollbar-base-color        | yes      | 设置滚动条主要构成部分的颜色           |
| scrollbar-track-color       | yes      | 设置滚动条轨迹组成部分的颜色           |

#### ensure的使用

require,ensure和webpack.ensure 在vue项目中的功能是相同的，它的作用激素hi异步加载---代码分割

ensure会把没有使用过的require资源进行独立分成一个个js文件，require的第一个参数代表的就是当前这个require.ensure所依赖的其他异步加载模块 ，没有的话可以只用[]表示即可

For 循环中使用return continue 、break的区别

总结：在双层for循环中，在内循环中使用continue 只会在内循环中跳过此处循环，小循环继续执行，不会跳过大循环。

在双层for循环中，在内循环中使用break ，只会在内循环中结束本次循环，不会结束大循环；

在双层for循环中，在内循环中使用return ，结束本次大小循环，同时结束本方法。

拜读的书有：js高级程序设计书和代码整洁之道、react.js深入浅出等书籍

底下出现了问题

有几个项目需要去看，一个是宽带测速的，一个是人脸识别插件的 

还有一个xflow的整体项目

D:\projectProgress\chognqing\GridSandTableBigScreenWeb\src\components\grid-big-screen\one\medium-box\middle.vue  这个是网格沙盘项目的

手机端还有一个用户行为的使用

two和three还没有看

```js

```



什么时候把自己写的比较好的组件拿出去，哈哈哈， 真好，反正离职前一定要准备一下；

还有一个颜色改变器这个组件有必要写一下，还有ol和echarts地图这两个得看一下
https://blog.csdn.net/qq_43039004/article/details/110927575
javascript:(function(){document.querySelector('#userName').value = `huangxiongfa`;document.querySelector('#password').value = `SDVtest@159`;document.querySelector('#userName').dispatchEvent(new Event('input'));document.querySelector('#password').dispatchEvent(new Event('input'));console.log('ok');}());
